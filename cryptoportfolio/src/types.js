/**
 * @typedef {{id: number, completed: boolean, title: string}} RateItem
 */
export var RateItem;

/**
 * @typedef {Array<RateItem>} RateItemList
 */
export var RateItemList;

/**
 * @typedef {Object} WalletItems
 * @property {number=} BTC
 * @property {number=} ETH
 * @property {number=} XRF
 * @property {number=} MNR
 */
export var WalletItems;

/**
 * @typedef {Array<WalletItem>} WalletItemList
 */
export var WalletItemList;
